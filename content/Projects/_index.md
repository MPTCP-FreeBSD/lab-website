---
title: Ongoing Projects
type: section
layout: list

# Gives every project page `type: project`, which is what routes them to
# layouts/project/single.html. Set here rather than in each project's front
# matter so new projects pick it up automatically - and so the directory's
# capital "P" never has to be matched by a layout path, which would break on
# the case-sensitive Linux build box.
cascade:
  type: project
---
